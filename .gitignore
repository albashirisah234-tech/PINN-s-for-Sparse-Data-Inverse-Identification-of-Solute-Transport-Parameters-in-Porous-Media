__pycache__/
*.pyc
*.pt
.ipynb_checkpoints/

# Results are regenerated by the experiments/ scripts -- keep the repo
# itself light. Remove these two lines if you'd rather track results in
# git directly (they're small CSVs/PNGs, so either choice is reasonable).
results/tables/*.csv
results/figures/*.png
